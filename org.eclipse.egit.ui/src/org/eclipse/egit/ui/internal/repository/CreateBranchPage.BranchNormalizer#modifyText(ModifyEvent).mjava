		@Override
		public void modifyText(ModifyEvent e) {
			nameText.setFocus();
			if (listenerActive)
				return;
			try {
				listenerActive = true;
				normalize();
			} finally {
				listenerActive = false;
			}
		}

