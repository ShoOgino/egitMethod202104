	/**
	 * Called when an item has been added.
	 *
	 * @param index
	 *            table index of the new item
	 * @param rev
	 *            of the new item
	 */
	void itemAdded(int index, RevObject rev);

