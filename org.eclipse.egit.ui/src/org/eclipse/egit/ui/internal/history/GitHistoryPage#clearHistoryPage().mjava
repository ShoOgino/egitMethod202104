	private void clearHistoryPage() {
		setCurrentRepo(null);
		selectedObj = null;
		name = ""; //$NON-NLS-1$
		input = null;
		commitAndDiff.getCommitViewer().setInput(null);
		fileViewer.newInput(null);
		setInput(null);
	}

