	@Override
	public void modifyText(ModifyEvent e) {
		Text text = (Text) e.widget;
		text.setFocus();
		if (listenerActive)
			return;
		try {
			listenerActive = true;
			normalize(text);
		} finally {
			listenerActive = false;
		}
	}

