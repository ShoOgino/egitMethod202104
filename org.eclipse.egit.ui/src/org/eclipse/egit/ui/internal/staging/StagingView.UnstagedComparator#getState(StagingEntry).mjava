		private int getState(StagingEntry entry) {
			switch (entry.getState()) {
			case UNTRACKED:
				return 1;
			case MISSING:
				return 2;
			case MODIFIED:
				return 3;
			default:
				return super.category(entry);
			}
		}

