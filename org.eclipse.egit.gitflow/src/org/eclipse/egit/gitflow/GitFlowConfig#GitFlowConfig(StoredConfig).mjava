	/**
	 * Creates a {@link GitFlowConfig}
	 *
	 * @param config
	 *            to read values from
	 * @since 5.6
	 */
	public GitFlowConfig(@NonNull StoredConfig config) {
		this.config = config;
	}

